[project]
name = "codex-usage-widget"
version = "0.1.0"
description = "Always-on-top Codex remaining usage widget"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"